[settings]
# mise.unix.toml / mise.windows.toml for clang vs MSVC and m4 vs m2-m4.
auto_env = true

[tools]
go = "1.26.5"
"conda:cmake" = { version = "3.31.6", channel = "conda-forge" }
"conda:ninja" = { version = "1.12.1", channel = "conda-forge" }
